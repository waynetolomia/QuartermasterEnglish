rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  
    // Open rules for GitHub Pages usage (No Firebase Auth)
    match /users/{userId} {
      allow read, write: if true;
    }
    
    match /vocabulary_master/{vocabId} {
      allow read, write: if true;
    }
  }
}